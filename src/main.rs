use core::slice;

use chrono;
use chrono::Datelike; // 0.4.19
                      // use std::cmp::Ordering;
fn main() {
    // push_string();
    // range_sample();
    // mutable_slice();
    // open_ended_slice();
    range_inclusive();
}
